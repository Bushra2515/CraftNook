<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>My Products</title>
  </head>
  <body>
    <h2>My Products</h2>
    <a href="/seller/addproduct">➕ Add New Product</a> |
    <a href="/logout">🚪 Logout</a>

    <% if (products.length === 0) { %>
    <p>You haven’t added any products yet.</p>
    <% } else { %>
    <ul>
      <% products.forEach(p => { %>
      <li>
        <h3><%= p.name %></h3>
        <p><%= p.description %></p>
        <p>Price: $<%= p.price %></p>
        <p>Category: <%= p.category %></p>
        <p>Location: <%= p.location %></p>
      </li>
      <% }) %>
    </ul>
    <% } %>
  </body>
</html>
